# Analysis helpers

This directory contains optional analysis scripts that are not part of the core pipeline execution.

Add project specific datasets (for example external FOX labels) locally and do not commit sensitive data.
